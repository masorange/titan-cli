name: "Extended Workflow Name"
description: "What this workflow adds"
extends: "plugin:git/commit-ai"

hooks:
  before_commit:
    - id: example_step
      name: "Example Step"
      plugin: project
      step: example_step
      on_error: continue
