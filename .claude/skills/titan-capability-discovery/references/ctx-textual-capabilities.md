# ctx.textual Capabilities

When creating a project step, remember `ctx.textual` can support:

1. step lifecycle messages
2. normal, dim, success, warning, and error text
3. loading indicators
4. prompts and confirmations
5. mounted widgets such as panels and tables

Do not add a custom UI layer when `ctx.textual` already covers the workflow need.
