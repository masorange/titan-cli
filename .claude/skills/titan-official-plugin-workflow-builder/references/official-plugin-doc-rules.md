# Official Plugin Doc Rules

If a workflow change exposes a new public plugin capability, or changes how a public client function should be used, update the corresponding docs page under `docs/plugins/`.

At minimum document:

1. what the capability does
2. how it is called or triggered
3. required parameters
4. optional parameters
5. important usage constraints
