# Official Plugin Decision Flow

1. Is this a workflow addition to an existing official plugin?
2. Does an existing workflow already cover most of the behavior?
3. Can existing plugin steps be reused or extended?
4. Is a new step enough, or is new business logic required?
5. If business logic is needed, should it become an operation?
6. If the plugin public API changes, update docs too.
