# Official Plugin Workflow Layout

Official plugin workflows live under the plugin package:

```text
plugins/titan-plugin-<name>/titan_plugin_<name>/workflows/
```

Steps usually live under:

```text
plugins/titan-plugin-<name>/titan_plugin_<name>/steps/
```

Supporting layers live beside them under the plugin package using the established architecture.
